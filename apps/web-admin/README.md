# Kambuja POS Web Admin

React application for ADMIN shop management and CASHIER point-of-sale workflows.

## Run

```bash
npm install
npm run dev
```

The Vite configuration reads `VITE_API_BASE_URL` from the repository root `.env`.
