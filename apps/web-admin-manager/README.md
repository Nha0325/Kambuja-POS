# Kambuja POS Web Admin Manager

ADMIN_MANAGER-only React application for platform administration.

## Run

```bash
npm install
npm run dev
```

The Vite configuration reads `VITE_API_BASE_URL` from the repository root `.env`.
