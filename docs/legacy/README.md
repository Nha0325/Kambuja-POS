# Legacy archive

This directory preserves files removed from the active runtime structure.

- `old-public-pages/` contains the previous directly accessed procedural pages.
- `old-root-files/` contains superseded root configuration, scripts, and tools.

These files are retained for migration reference and are not loaded by the MVC
entry point.
