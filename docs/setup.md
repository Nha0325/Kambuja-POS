# Setup

1. Install PHP, Composer, MySQL, and the PDO MySQL extension.
2. Run `composer install`.
3. Copy `.env.example` to `.env`.
4. Set the database values in `.env`.
5. Import `database/schema.sql`.
6. Run `php -S localhost:8000 -t public`.
