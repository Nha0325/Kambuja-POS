[build]
builder = "RAILPACK"
buildCommand = "cd Backend && npm ci --no-audit --no-fund"

[deploy]
startCommand = "cd Backend && npm run seed && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
