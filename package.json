{
  "name": "kambuja-pos-railway-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd Backend && npm ci --no-audit --no-fund",
    "start": "cd Backend && npm start"
  }
}
